## What

<!-- Brief description of the change -->

## Why

<!-- Motivation — what problem does this solve? -->

## Test plan

- [ ] `npm test` passes
- [ ] `npm run build` succeeds
- [ ] Tested with `node dist/cli.mjs` (if CLI changes)
